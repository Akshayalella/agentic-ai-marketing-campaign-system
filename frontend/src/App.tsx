import {Routes,Route,Link,Navigate,useLocation} from 'react-router-dom';
import Dashboard from './pages/Dashboard'; import Campaigns from './pages/Campaigns'; import CreateCampaign from './pages/CreateCampaign'; import Research from './pages/Research'; import Strategy from './pages/Strategy'; import ContentStudio from './pages/ContentStudio'; import ContentCalendar from './pages/ContentCalendar'; import Analytics from './pages/Analytics'; import Reports from './pages/Reports';
const links=[['/','Dashboard'],['/campaigns','Campaigns'],['/create','Create Campaign'],['/research','Audience Research'],['/strategy','Campaign Strategy'],['/content','Content Studio'],['/calendar','Content Calendar'],['/analytics','Analytics'],['/reports','Reports']];
export default function App(){const loc=useLocation();return <div className="app"><aside><div className="brand">Campaign<span>AI</span></div>{links.map(([p,n])=><Link className={loc.pathname===p?'active':''} to={p} key={p}>{n}</Link>)}</aside><main><header><div><small>Agentic AI Marketing Platform</small><h1>{links.find(x=>x[0]===loc.pathname)?.[1]||'Dashboard'}</h1></div><div className="pill">Demo Mode</div></header><section className="page"><Routes><Route path="/" element={<Dashboard/>}/><Route path="/campaigns" element={<Campaigns/>}/><Route path="/create" element={<CreateCampaign/>}/><Route path="/research" element={<Research/>}/><Route path="/strategy" element={<Strategy/>}/><Route path="/content" element={<ContentStudio/>}/><Route path="/calendar" element={<ContentCalendar/>}/><Route path="/approvals" element={<Navigate to="/content" replace/>}/><Route path="/analytics" element={<Analytics/>}/><Route path="/reports" element={<Reports/>}/></Routes></section></main></div>}
