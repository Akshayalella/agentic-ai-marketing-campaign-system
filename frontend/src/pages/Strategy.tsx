import {useState} from 'react'; import {getActiveCampaignId} from '../services/campaign'; import {api} from '../services/api';
export default function Strategy(){const active=getActiveCampaignId(); const [id,setId]=useState(active?String(active):''); const [s,setS]=useState<any>(); if(!active && !id) return <div className="card"><h3>Select a campaign first</h3><p className="muted">Go to Campaigns and select a campaign, then return here.</p></div>; return <div className="card"><h3>Campaign strategy</h3><input value={id} onChange={e=>setId(e.target.value)}/><button className="button" onClick={()=>api.get(`/api/campaigns/${id}/strategy`).then(x=>setS(x.data))}>Generate/view strategy</button>{s&&<div className="grid" style={{marginTop:18}}><div className="card"><b>Theme</b><p>{s.theme}</p></div><div className="card"><b>Audience</b><p>{s.audience_segments?.map((x:any)=>x.name).join(', ')}</p></div><div className="card"><b>Timeline</b><p>{s.timeline_days} days</p></div><div className="card"><b>KPIs</b><p>{s.kpis.join(', ')}</p></div><div className="card"><b>Budget allocation</b><pre>{JSON.stringify(s.budget_allocation,null,2)}</pre></div></div>}</div>}