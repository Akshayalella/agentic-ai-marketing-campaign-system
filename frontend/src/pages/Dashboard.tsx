import {useEffect,useState} from 'react'; import {campaigns} from '../services/api';
export default function Dashboard(){const [data,setData]=useState<any[]>([]);useEffect(()=>{campaigns().then(setData).catch(()=>{})},[]);return <><div className="hero"><h2>AI-powered campaign operations</h2><p className="muted">Run the six-agent workflow from requirements to approved content and measurable campaign reports.</p></div><div className="grid"><div className="card"><div className="muted">Campaigns</div><div className="big">{data.length}</div></div><div className="card"><div className="muted">Active</div><div className="big">{data.filter(x=>x.status==='active').length}</div></div><div className="card"><div className="muted">Approval workflow</div><div className="big">Human-in-loop</div></div></div><div className="card" style={{marginTop:18}}><h3>Six-agent pipeline</h3><p className="muted">Requirements → Research → Strategy → Content → Compliance → Analytics</p></div></>}
