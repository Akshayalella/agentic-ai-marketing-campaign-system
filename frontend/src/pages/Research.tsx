import {useState} from 'react'; import {getActiveCampaignId} from '../services/campaign'; import {api} from '../services/api';
export default function Research(){const active=getActiveCampaignId(); const [id,setId]=useState(active?String(active):''); const [r,setR]=useState<any>(); if(!active && !id) return <div className="card"><h3>Select a campaign first</h3><p className="muted">Go to Campaigns and select a campaign, then return here.</p></div>; return <div className="card"><h3>Audience & competitor research</h3><input value={id} onChange={e=>setId(e.target.value)}/><button className="button" onClick={()=>api.get(`/api/campaigns/${id}/research`).then(x=>setR(x.data))}>Load research</button>{r&&<div>{r.personas?.map((p:any)=><div className="card" key={p.name}><b>{p.name}</b><p className="muted">{p.description} · {p.verified?'Verified':'Hypothetical'}</p></div>)}{r.sources?.map((s:any)=><div className="card" key={s.url}><b>{s.title}</b><p>{s.summary}</p><a href={s.url} target="_blank" rel="noreferrer">Source</a></div>)}</div>}</div>}